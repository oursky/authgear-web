{
  "name": "authgear-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names cms,frontend --prefix-colors magenta,cyan \"npm run dev --prefix cms\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
